import { StyleSheet } from "react-native";

const listingEditScreenStyles = StyleSheet.create({
  form: {
    flex: 1,
    justifyContent: "center",
    padding: 20,
    gap: 20,
  },
  button: {
    marginTop: 10,
  },
});

export default listingEditScreenStyles;
