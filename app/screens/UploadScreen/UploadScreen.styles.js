import { StyleSheet } from "react-native";

const uploadScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
});

export default uploadScreenStyles;
